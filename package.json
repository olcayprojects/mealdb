{
  "homepage": "https://olcayprojects.github.io/mealdb/",
  "name": "mealdb",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint",
    "predeploy": "npm run build",
    "deploy": "npm run build && touch ./out/.nojekyll && gh-pages -d out -t true"
  },
  "dependencies": {
    "next": "15.1.3",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "touch-cli": "^0.0.1"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "postcss": "^8",
    "tailwindcss": "^3.4.1"
  }
}
